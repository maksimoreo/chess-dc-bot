source 'https://rubygems.org'

# Originally used with ruby 2.7.0
ruby '3.2.1'

# Used as interface between Discord users and chess game (chess_bot_base.rb)
gem 'discordrb'

# Used to render chessboard image (chessboard_printer/chessboard_printer.rb)
gem 'rmagick'

gem 'rubocop', require: false
